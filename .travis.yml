sudo: false

git:
    submodules: false
    
addons:
  apt:
    packages:
    - build-essential
    - libxml2
    - libssh2-1-dev
    - clang
    - cmake

before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive
  
language: c++

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
